# Used by the integration test suite (tests/integration). Point this at a
# DEDICATED database — the integration tests wipe and repopulate it.
TEST_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/mini_saas_test"
JWT_SECRET="test-secret-not-for-production"
